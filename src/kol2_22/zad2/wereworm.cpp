#include <vector>
#include <deque>

int wereworm(const std::vector<int> vect) {
    std::deque<int> auxiliary;
    int sum = 0;
    std::vector<int>::iterator it;
    int i;

    while (vect.size() - i + auxiliary.size() > 2) {
        /* code */
    }
}